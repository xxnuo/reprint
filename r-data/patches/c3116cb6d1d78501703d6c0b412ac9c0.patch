rustup 1.25.1 (bb60b1e89 2022-07-12)
Rust工具链的安装程序

使用方法:
    rustup [FLAGS] [+toolchain] <SUBCOMMAND>

FLAGS:
    -v, --verbose    启用粗略的输出
    -q, --quiet      禁用进度输出
    -h, --help       打印帮助信息
    -V, --version    打印版本信息

ARGS:
    <+toolchain> 发布通道（例如：+stable）或自定义工具链来设置覆盖。

SUBCOMMANDS:
    show           显示活动的和已安装的工具链或配置文件
    update         更新Rust工具链和rustup
    check          检查Rust工具链和rustup的更新情况
    default        设置默认的工具链
    toolchain      修改或查询已安装的工具链
    target         修改一个工具链的支持目标平台
    component      修改一个工具链的已安装组件
    override       修改目录中的工具链重写项
    run            在为给定的工具链配置的环境下运行一个命令
    which          显示哪个二进制文件将被运行在一个给定的命令中
    doc            打开当前工具链的文档
    self           修改rustup的安装
    set            改变rustup的设置
    completions    为你的shell生成tab补全脚本
    help           打印此信息或给定子命令的帮助

DISCUSSION:
    Rustup从官方的发布渠道安装Rust编程语言，
    使你能在稳定版、测试版和夜间版（stable,beta,nightly）
    编译器之间轻松切换，并保持更新。
    它使交叉编译变得更加简单，
    为常见的平台提供了二进制的标准库构建。

    如果你是Rust的新手，可以考虑运行`rustup doc --book`
    来学习Rust。
